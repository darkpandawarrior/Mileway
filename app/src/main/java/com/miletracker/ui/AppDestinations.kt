package com.miletracker.ui

/**
 * Top-level nav-graph routes for the four bottom-navigation tabs. Each maps to a feature
 * module's nested graph (see [MileTrackerAppRoot]).
 */
object AppGraph {
    const val TRACK = "track_graph"
    const val LOG = "log_graph"
    const val MEDIA = "media_graph"
    const val PROFILE = "profile_graph"
}
